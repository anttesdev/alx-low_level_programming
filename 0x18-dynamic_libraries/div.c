#include <stdio.h>

/**
 * div - a function that divides one number with another
 * @a: first number
 * @b: second number
 * Return: the quotient of the numbers
 */

int div(int a, int b)
{
	int c;

	c = a / b;
	return (c);
}
