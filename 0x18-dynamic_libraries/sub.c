#include <stdio.h>

/**
 * sub - a function that subtracts one number from another
 * @a: first number
 * @b: second number
 * Return: the difference of the numbers
 */

int sub(int a, int b)
{
	return (a - b);
}
