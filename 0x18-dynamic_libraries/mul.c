#include <stdio.h>

/**
 * mul - a function that multiplies two numbers
 * @a: first number
 * @b: second number
 * Return: the product of the numbers
 */

int mul(int a, int b)
{
	int c;

	c = a * b;
	return (c);
}
