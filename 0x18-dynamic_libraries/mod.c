#include <stdio.h>

/**
 * mod - a function that gives the remainder of one number divided
 * with another
 * @a: first number
 * @b: second number
 * Return: the remainder of the numbers
 */

int mod(int a, int b)
{
	int c;

	c = a % b;
	return (c);
}
